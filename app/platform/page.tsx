import type { Metadata } from 'next';
import { Activity, Braces, ClipboardCheck, GitCompareArrows, Radio, ShieldCheck } from 'lucide-react';
import { PageIntro } from '@/components/InstitutionalShell';
import { PageShell } from '@/components/PageChrome';
export const metadata: Metadata = { title:'Platform Overview | QSentia' };
const capabilities=[['Model telemetry','Normalize portfolio, decisions, positions, orders, and model health into one review surface.',Activity],['Research validation','Compare reported outcomes, source history, drawdown, and benchmark behavior.',GitCompareArrows],['Paper/live monitoring','Keep operating mode, account status, positions, and execution evidence visible.',Radio],['Broker readiness','Review entitlement, broker, scheduler, and risk gates before execution is enabled.',ShieldCheck],['API access','Issue scoped credentials and expose documented model and dashboard endpoints.',Braces],['Audit trails','Record commercial, credential, deployment, and operating changes for review.',ClipboardCheck]];
export default function PlatformPage(){return <PageShell active="/platform"><PageIntro eyebrow="Platform lane" title="The operating layer for systematic models" body="QSentia brings research validation, model telemetry, paper and live monitoring, broker readiness, API access, and auditable controls into one platform."/><section className="mx-auto max-w-7xl px-4 py-12 sm:px-6"><div className="grid gap-px overflow-hidden rounded-[10px] border border-[#e2e7fb] bg-[#e2e7fb] md:grid-cols-2 lg:grid-cols-3">{capabilities.map(([title,body,Icon])=><div key={String(title)} className="bg-white p-6"><Icon className="h-5 w-5 text-[#3d52da]"/><h2 className="mt-5 text-xl font-semibold text-[#06130c]">{String(title)}</h2><p className="mt-3 text-sm leading-6 text-[#5a685f]">{String(body)}</p></div>)}</div></section></PageShell>}
