import type { Metadata } from 'next';
import Link from 'next/link';
import { Braces, KeyRound, Radio, Webhook } from 'lucide-react';
import { PageIntro } from '@/components/InstitutionalShell';
import { PageShell, SectionCard } from '@/components/PageChrome';
export const metadata: Metadata = { title:'Developer Center | QSentia' };
const cards=[['API key flow','Admin-issued credentials are stored as hashes and scoped by entitlement.',KeyRound],['Model cards','GET /api/models and GET /api/models/{slug} expose published model metadata.',Braces],['Telemetry','GET /api/dashboard returns repository-backed performance and operating state.',Radio],['Webhooks','Customer webhook state is reserved for configured deployments; it is not enabled by default.',Webhook]];
export default function DevelopersPage(){return <PageShell active="/developers"><PageIntro eyebrow="Developer center" title="Build against explicit contracts" body="Use the endpoint guide, model-card schema, credential lifecycle, and operational boundaries required for a production integration."/><section className="mx-auto max-w-7xl px-4 py-12 sm:px-6"><div className="grid gap-5 md:grid-cols-2">{cards.map(([title,body,Icon])=><SectionCard key={String(title)} className="p-6"><Icon className="h-5 w-5 text-[#3d52da]"/><h2 className="mt-4 text-xl font-semibold text-[#06130c]">{String(title)}</h2><p className="mt-3 text-sm leading-6 text-[#5a685f]">{String(body)}</p></SectionCard>)}</div><div className="mt-8 flex flex-wrap gap-3"><Link href="/docs" className="rounded-md bg-[#172554] px-5 py-3 text-sm font-semibold text-white">Read API guide</Link><a href="/openapi.json" className="rounded-md border border-[#cbd5ff] bg-white px-5 py-3 text-sm font-semibold text-[#172554]">OpenAPI specification</a></div></section></PageShell>}
