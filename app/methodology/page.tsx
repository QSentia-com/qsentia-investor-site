import type { Metadata } from 'next';
import { Activity, Bot, Database, Gauge, Radio, ShieldCheck } from 'lucide-react';
import { PageIntro } from '@/components/InstitutionalShell';
import { PageShell } from '@/components/PageChrome';
export const metadata: Metadata = { title:'Investment Methodology | QSentia' };
const steps=[['Signal inputs','Market, text, sentiment, and model-specific source inputs are ingested by each registered strategy.',Database],['Model inference','Approved model code transforms source inputs into a directional or allocation signal.',Bot],['Confidence scoring','The model reports confidence and component evidence where those fields are available.',Gauge],['Risk sizing','Target exposure is constrained by the strategy implementation and approved account controls.',ShieldCheck],['Execution','Paper or live orders are routed through the configured broker only after applicable gates pass.',Radio],['Monitoring','Portfolio, decision, position, and order logs flow into telemetry for review and reconciliation.',Activity]];
export default function MethodologyPage(){return <PageShell active="/methodology"><PageIntro eyebrow="Investment process" title="From information to monitored execution" body="QSentia separates research, signal generation, risk sizing, execution, and evidence so each stage can be inspected without pretending the model is a black box."/><section className="mx-auto max-w-7xl px-4 py-12 sm:px-6"><div className="grid gap-px overflow-hidden rounded-[10px] border border-[#e2e7fb] bg-[#e2e7fb] md:grid-cols-2">{steps.map(([title,body,Icon],index)=><div key={String(title)} className="bg-white p-6"><div className="flex items-center justify-between"><span className="flex h-10 w-10 items-center justify-center rounded-md bg-[#eef2ff] text-[#3d52da]"><Icon className="h-5 w-5"/></span><span className="text-xs font-bold text-[#94a3b8]">0{index+1}</span></div><h2 className="mt-5 text-xl font-semibold text-[#06130c]">{String(title)}</h2><p className="mt-3 text-sm leading-6 text-[#5a685f]">{String(body)}</p></div>)}</div></section></PageShell>}
